rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Disallow all client-side read/write. 
      // Admin SDK (backend) will still have full access.
      allow read, write: if false;
    }
  }
}
